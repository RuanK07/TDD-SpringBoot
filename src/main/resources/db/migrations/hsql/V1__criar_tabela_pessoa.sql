CREATE SEQUENCE pessoa_id_seq AS BIGINT STARTNWITH 1 INCREMENT BY 1;
CREATE TABLE pessoa (
	id BIGINT GENERATED BY DEFAULT AS SEQUENCE pessoa_id_seq PRIMARY KEY,
	nome VARCHAR(80) NOT NULL,
	cpf VARCHAR(80) NOT NULL
);